module.exports = {
  name: "rules",
  category: "info",
  permissions: [],
  devOnly: false,
  run: async ({client, message, args}) => {
    message.reply(
      
   `1. Don't spam
    2. Don't spam
    3. Don't spam
    4. Don't spam`)
  }
}